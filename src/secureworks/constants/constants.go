package constants

func foo() {
	/*
	  strictly a place holder, go generate will clobber this file
	  it was simplest to just make a dummy file and add to git to ensure its existence
	  instead of mucking with paths
	*/
}
